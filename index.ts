import { registerRootComponent } from "expo";
import App from "./App";

// Expo entry point. registerRootComponent wraps App and registers it with
// AppRegistry so it runs in both Expo Go and a standalone build.
registerRootComponent(App);
